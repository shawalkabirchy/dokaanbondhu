# Proof log

Results of the proof tasks P1 to P10 (spec 1.5). One row per run; a failed proof is logged too, and the
specification is changed before the code.

| Date       | Proof | What was run                                                                                                                                                                                                                                                                                                  | Result                                                                                                                                                                                                                | Versions                                                                             |
| ---------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 2026-09-26 | P1    | On `shop-dev` after migrate, roles, seed and check: `psql` from Docker (`postgres:17`) as `dokaanbondhu_ro.<ref>` through the session pooler (port 5432) with `sslmode=verify-full` and `certs/supabase-root-2021.crt`; a `SELECT`, an `INSERT`, a read of `users`; the same login again with the `pg` driver | **Passed.** TLS 1.3 verified; `SELECT` works (239 parts, Rahim Motors due 19,200 taka); `INSERT` and `UPDATE` fail with 25006 "cannot execute ... in a read-only transaction"; `users` fails with "permission denied" | Server PostgreSQL 17.6 (Supabase, Singapore); psql 17.11; pg 8.23.0; Node.js 24.14.0 |
